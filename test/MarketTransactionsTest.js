var ZoneRecovery = {};
var MarketTransactions = require("../app/js/MarketTransactions");
var expect = require("chai").expect;

describe('Balance counter displayed', function() {
  describe('Clients balance will be diplayed on page load', function () {
    it('will display the clients balance when market transaction page loads', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Profit/loss counter displayed', function() {
  describe('Clients profit/loss will be diplayed on page load', function () {
    it('will display the clients profit/loss when market transaction page loads', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Logout button works', function() {
  describe('Logout button logs the client out when clicked', function () {
    it('will log the client out of their account and end their session on click', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Market box gets populated', function() {
  describe('Market box gets populated with volatile markets', function () {
    it('will display all volatile markets', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Spinner increases number', function() {
  describe('Spinner increases numbers one at a time when clicked on the up arrow', function () {
    it('will display a number increased by one', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Current positions link redirects the client', function() {
  describe('Current positions tab to redirect clients to that page', function () {
    it('will redirect clients to the current positions page', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Submit button sends trade with buy', function() {
  describe('Submit button to send the users buy position', function () {
    it('will send the buy position on submit button click', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Submit button sends trade with sell', function() {
  describe('Submit button will send the users sell position', function () {
    it('will send the sell position on submit button click', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Receive confirm on deal success', function() {
  describe('Client to receive a green message confirming a deal when successful', function () {
    it('will display a confirm message', function() {
      expect().to.not.equal(null);
    });
  });
});

describe('Receive rejection message on deal rejection', function() {
  describe('Client to receive a red message rejecting a deal when rejected', function () {
    it('will display a rejection message', function() {
      expect().to.not.equal(null);
    });
  });
});
